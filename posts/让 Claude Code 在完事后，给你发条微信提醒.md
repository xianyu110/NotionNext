---
title: "让 Claude Code 在完事后，给你发条微信提醒"
date: Fri Jan 02 2026 23:55:01 GMT+0800 (China Standard Time)
category: "人工智能"
tags: ["#微信"]
summary: ""
author: "xianyu120"
status: "Published"
---

* * *

#### **引言：一个“不弱”的需求**

今天在 Claude CodeX 交流群里看到群友 @郭昱Amy 抛出的一个问题：

> 新手有个很弱的问题 - Windows上怎么让claude code完成任务后通知我一下？现在每次都得我自己去主动看，经常它做完好一会了，我才发现。

其实，这个问题一点都不“弱”！我也有这个需求。

我会经常花很长的时间写一大段 prompt 然后扔给 Claude Code
去处理。通常需要5~10分钟，有时甚至要一小时多。当一个耗时较长的任务在运行时，我肯定不想一直盯着屏幕等待，但又担心错过完成的时机，从而因为一忙把某个项目搞忘了，也耽误了我继续压榨
Max 会员的价值。

**Claude Code 的问题，自然得用 Claude Code 解决。**

看到这个问题，我一眼就有了方案：**hooks 功能 + 虾推啥** 。

并且，我的第一反应不是自己上手开干，而是直接把思路扔给了 Claude Code，让它自己来解决这个问题！再次强调：不要把 Claude Code
只当作是一个写代码工具，它能干的有很多很多。

于是我直接在 Claude Code 中输入：

> 请搜索用虾推啥结合 claude code 的 hooks 来让 claude code
> 完成任务后通过微信通知用户的方案。给出详细的操作步骤，让所有人都可以无脑上手操作。

然后，Claude Code 就开始搜索相关资料，并最终给出了一个“较完整”的解决方案（“较完整”是因为我后面又呵斥了几句，并指点了几下……）。

下面是详细的配置指南，我已经 review、手动校对并实测通过。

> **注意** ：本指南仅适用于 Mac / Linux 用户，Windows 系统会稍有差异，可以把下面的内容扔给 AI，让它为你生成 Windows
> 版本的方案。

* * *

#### **实现 Claude Code + 虾推啥 微信通知🔔配置指南**

##### **一、准备工作**

###### **1.1 获取虾推啥 Token**

我们使用“虾推啥”（网址：`https://www.xtuis.cn/`）来实现微信通知。

  1. 用微信扫描下方二维码关注其公众号。
  2. 关注后，你将自动收到一个专属的 Token，格式类似：`0xOJ………………uMU`。请妥善保管这个 Token。

##### **二、创建通知脚本**

###### **2.1 创建脚本目录和文件**

在终端中执行以下命令，创建所需的目录和脚本文件。

    
    
    # 创建目录
    mkdir -p ~/.claude/hooks
    
    # 创建并写入脚本文件
    cat > ~/.claude/hooks/notify.sh << 'EOF'
    #!/bin/bash
    
    # ===============================================================
    # 重要：将 YOUR_TOKEN 替换为您从“虾推啥”获取的实际 Token
    # ===============================================================
    TOKEN="YOUR_TOKEN"
    
    URL="https://wx.xtuis.cn/${TOKEN}.send"
    
    # 获取当前时间
    TIME=$(date +"%Y-%m-%d %H:%M:%S")
    
    # 获取当前工作目录名作为会话标识
    SESSION=$(basename "$(pwd)")
    
    # 发送通知
    curl -s -X POST "$URL" \
      -d "text=✅ Claude Code 任务完成" \
      -d "desp=会话: ${SESSION}%0A时间: ${TIME}" \
      > /dev/null 2>&1
    
    exit 0
    EOF
    

###### **2.2 设置脚本权限**

为了让系统能够执行这个脚本，需要给它添加可执行权限。

    
    
    chmod +x ~/.claude/hooks/notify.sh
    

##### **三、配置 Claude Code Hooks**

###### **3.1 编辑配置文件**

> ⚠️ **特别注意：**  
>  在执行下面的命令前，请先检查 `~/.claude/settings.json` 文件是否存在。
>
>   * **如果文件不存在** ：可以直接执行下面的 `cat` 命令来创建。
>   * **如果文件已存在** ：请**不要** 直接执行 `cat` 命令，这会覆盖你原有的配置！你应该手动编辑该文件（例如使用 `vi
> ~/.claude/settings.json`），将 `"hooks"` 部分的内容添加进去。
>

    
    
    # 创建或编辑 Claude 配置文件
    cat > ~/.claude/settings.json << 'EOF'
    {
      "hooks": {
        "Stop": [
          {
            "matcher": "",
            "hooks": [
              {
                "type": "command",
                "command": "bash ~/.claude/hooks/notify.sh"
              }
            ]
          }
        ]
      }
    }
    EOF
    

##### **四、测试验证**

###### **4.1 手动测试脚本**

直接在终端运行脚本，看看是否能收到通知。

    
    
    bash ~/.claude/hooks/notify.sh
    

如果配置正确，你的微信应该会立刻收到一条来自“虾推啥”的测试通知。

###### **4.2 在 Claude Code 中测试**

现在，启动 Claude Code 并执行一个简单的任务。

    
    
    # 启动 Claude Code
    claude
    
    # 在 Claude Code 的交互界面中输入
    你好
    

或者直接通过命令行参数执行：

    
    
    claude 你好
    

等待任务完成后，检查你的微信是否收到了“✅ Claude Code 任务完成”的通知。

至此，全部配置完成。以后你的 Claude Code 在完成任务或请求权限时，你都会收到一条微信通知，再也不会错过了。

* * *

#### **写在最后：如何更好地使用 AI**

再次强调：**Claude Code 不只是代码生成器，它更是一个通用的问题解决助手。**

就像这次，我并没有用 Claude Code 来写什么复杂的代码，而是让它帮我解决了这个实际的 `hooks` 配置问题。

要发挥出 Claude Code 的真正作用，需要掌握三个关键要素：

  1. **找到钉子（明确的问题）**  
就像群友提出的“任务完成后如何通知”，这是一个具体、明确、有实际需求的问题。很多人用不好 AI 工具，是因为他们不知道要解决什么问题，只是盲目地想要“学习
AI”。但如果没有钉子，再好的锤子也无用武之地。我们生活和工作中处处都是“钉子”，要有意识地去发现它们。先从小的开始，解决一个真实的需求。

  2. **提供核心方案思路**  
我没有让 Claude Code 漫无目的地搜索“如何实现通知”，而是直接指出了“**虾推啥 + hooks**
”这个技术方案。因为我确定这个方案可行，并相信基于这些信息，Claude Code 能把活干好。如果你没有思路，那就多问 AI，用最好、最贵的
AI，并且要舍得投入。

  3. **迭代与优化**  
AI 的初版方案可能不完美，就像这次我还需要“呵斥几句，指点几下”。关键在于和 AI 协作，通过不断的追问和修正，共同打磨出最终的完美解决方案。

